package entry

// Entry is a minimal example value container used in examples.
// Note: the main cache uses cago.Entry; this package is independent.
type Entry struct {
    Name  string
    Value string
}
